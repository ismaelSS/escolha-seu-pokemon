import styled from 'styled-components';

export const PokemonNameStyle = styled.h3`
  font-family: "Archivo Black", sans-serif;
  font-style: normal;
  font-size: 40px;
  color:${({ theme }) => theme.colors.text1}
`
